app:
  data_dir: data/raw
  batch_size: 1000

mongodb:
  uri: mongodb://root:secret@localhost:27017/admin
  database: web_logs

jdbc:
  url: jdbc:postgresql://localhost:5433/etl_results
  user: etl
  password: secret
